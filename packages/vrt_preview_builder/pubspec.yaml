name: vrt_preview_builder
description: Internal builder for generating VRT preview manifests from Flutter @Preview annotations.
publish_to: 'none'

environment:
  sdk: ^3.12.1

dependencies:
  analyzer: ^8.4.0
  build: ^4.0.3
  dart_style: ^3.1.3
  glob: ^2.1.3
  pub_semver: ^2.2.0
